{
  "name": "supacatch-js",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "prepare": "effect-tsgo patch && lefthook install",
    "build": "bun run build:core && bun run build:runtimes && bun run build:adapters",
    "build:core": "bun run --cwd packages/core build",
    "build:runtimes": "bun run --cwd packages/node build && bun run --cwd packages/cloudflare build",
    "build:adapters": "bun run --cwd packages/bun build && bun run --cwd packages/tanstack-start build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "oxlint --fix",
    "lint:check": "oxlint",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "publint": "bun run --filter '*' publint",
    "check:versions": "bun scripts/check-package-versions.ts",
    "check": "bun run lint:check && bun run format:check && bun run check:versions && bun run build && bun run typecheck && bun run test && bun run publint"
  },
  "devDependencies": {
    "@effect/tsgo": "0.36.5",
    "@effect/vitest": "4.0.0-beta.103",
    "@types/node": "26.2.0",
    "effect": "4.0.0-beta.103",
    "lefthook": "2.1.10",
    "oxfmt": "0.63.0",
    "oxlint": "1.78.0",
    "publint": "0.3.23",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "packageManager": "bun@1.3.14"
}
